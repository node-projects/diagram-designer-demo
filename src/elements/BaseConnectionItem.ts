import { BaseCustomWebComponentConstructorAppend } from "@node-projects/base-custom-webcomponent";
import { IConnectionItem } from "../interfaces/IConnectionItem.js";

export abstract class BaseConnectionItem extends BaseCustomWebComponentConstructorAppend implements IConnectionItem {


   
    updateConnectionPosition(source, destination) {

    }
}